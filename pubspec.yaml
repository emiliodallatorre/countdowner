name: countdowner
description: Un'app di conto alla rovescia per date importanti.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1


environment:
  sdk: ">=2.15.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.4

  # Utility
  json_annotation: ^4.4.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.4

  # Serializzazione
  json_serializable: ^6.1.3
  build_runner: ^2.1.7


flutter:
  uses-material-design: true

